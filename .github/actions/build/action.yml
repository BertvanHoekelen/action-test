name: Build
author: Bert van Hoekelen
description: Builds the application
branding:
  icon: terminal
  colorr: black
inputs:
  ssh-key:
    description: SSH key that has access to required composer files
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
